# Provenance note (bootstrap)

The full `PROVENANCE.md`, integrity manifests (`integrity/SHA256SUMS`,
`integrity/file-manifest.json`), and `ASSET-INVENTORY.json` arrive with the
Stage 2 release upload; they hash every file in the tree and therefore only
make sense once the complete tree is present. Until then, the canonical
release archive is `lamplight-1.2.1-source.zip`, SHA-256
ffb2786ec5dc77c8c487b51cc974be6669193bd398aaecce75a7b7a...
(full value recorded in the release archive's .sha256 sidecar).

This file will be deleted when Stage 2 completes.
